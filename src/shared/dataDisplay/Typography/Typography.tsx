import { StyledTypography } from 'shared/dataDisplay/Typography/styles';
import { TypographyProps } from 'shared/dataDisplay/Typography/types';

export const Typography = (props: TypographyProps) => {
	return <StyledTypography {...props} />;
};
