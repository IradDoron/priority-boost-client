import { StackProps } from 'shared/layout/Stack/types';

import { StyledStack } from 'shared/layout/Stack/styles';

export const Stack = (props: StackProps) => {
	return <StyledStack {...props} />;
};
