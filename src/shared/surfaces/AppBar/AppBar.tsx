import { StyledAppBar } from 'shared/surfaces/AppBar/styles';
import { AppBarProps } from 'shared/surfaces/AppBar/types';

export const AppBar = (props: AppBarProps) => {
	return <StyledAppBar {...props} />;
};
